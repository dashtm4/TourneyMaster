const config = {
  // DEFAULT_CURRENCY: 'usd',
  DEFAULT_SKU_INVENTORY_TYPE: 'finite',
  DEFAULT_SKU_INVENTORY_QUANTITY: '500',
  STRIPE_API_SECRET_KEY: process.env.STRIPE_SECRET_KEY,
};

export default config;
