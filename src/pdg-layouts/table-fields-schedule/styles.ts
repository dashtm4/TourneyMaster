import { StyleSheet } from '@react-pdf/renderer';

const styles = StyleSheet.create({
  page: {
    padding: 15,
  },
  tableContainer: {
    flexGrow: 1,
    marginBottom: 15,
  },
  facilityTitle: {
    flexDirection: 'row',
    marginBottom: 10,
  },
  scheduleDate: {
    marginRight: 20,
  },
});

export { styles };
