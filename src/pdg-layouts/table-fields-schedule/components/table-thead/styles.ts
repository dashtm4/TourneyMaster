import { StyleSheet } from '@react-pdf/renderer';

const styles = StyleSheet.create({
  thead: {
    marginLeft: 115,
    padding: 5,
    textAlign: 'center',
    color: 'white',
    backgroundColor: 'dodgerblue',
  },
});

export { styles };
