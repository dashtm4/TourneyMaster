import { StyleSheet } from '@react-pdf/renderer';

const styles = StyleSheet.create({
  thead: {
    flexDirection: 'row',
    marginLeft: 100,
  },
  fieldName: {
    width: 160,
    height: 30,

    margin: '0 15 0 0',
    padding: '0 15px',
  },
});

export { styles };
