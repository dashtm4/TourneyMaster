import React from 'react';
// import { Button, Select, Tooltip, CardMessage } from 'components/common'

const ScoringFilter = () => (
  <h3 className="visually-hidden">Scoring filters</h3>
);

export default ScoringFilter;
